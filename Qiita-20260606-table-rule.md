<!--
Qiita 技術記事ドラフト基盤
作成日: 2026-06-06
テーマ: aira バーチャルロボットレースで「テーブルモード」と「ルールベースモード」はどこまで速く走れるか
対象: Robot1 単独トライアル
このファイルは記事の土台。実験を進めながら追記・検証していく。
-->

# 【aira】テーブルモード vs ルールベース、自律走行はどこまで速くなるか — Robot1 単独トライアル

> aira（Autonomous Intelligence Racing Arena）の Unity シミュレータ上で、
> 「決め打ち再生（テーブルモード）」と「画像フィードバック制御（ルールベース）」の
> 2 つのアプローチがそれぞれどこまで速く走れるかを、実験しながら検証する記事。

---

## 0. この記事で扱うこと / 扱わないこと

- 扱う: テーブルモード（CSV 再生 / オープンループ）の到達点と限界、ルールベース（P制御 / クローズドループ）の改善余地
- 扱う: aira を自動で「レース → 解析 → パラメータ/CSV 更新 → レース」と回す自動最適化ループの設計
- 扱わない（今回）: AI モード（ニューラルネット推論）、RL 学習、Robot2 との同時対戦

---

## 1. aira の仕組み（前提知識）

### 1.1 システム構成

```
Unity シミュレータ (Server)
   │  50ms ごと (20fps) に画像と SOC を出力
   ▼
data_interactive/  latest_RGB (224×224 JPEG) / latest_SOC.txt
   │
   ▼  Python Client (main.py)
制御モジュール（モードで切替）
   ├─ keyboard      … 手動（キーボード）
   ├─ table         … CSV の値を上から順に再生（オープンループ）★
   ├─ rule_based    … 画像から白線検出して操舵（クローズドループ）★
   ├─ ai            … ニューラルネット推論
   └─ smartphone    … スマホコントローラ
   │
   ▼  WebSocket
Unity に {driveTorque, steerAngle} を送信
```

### 1.2 入出力（MIMO 制御）

| 種別 | データ | 詳細 |
|------|--------|------|
| 入力① 画像 | RGB | 224×224 JPEG（車載カメラ）|
| 入力② バッテリ | SOC | 0.0〜1.0 |
| 出力① スロットル | `drive_torque` | -1.0〜+1.0 |
| 出力② 操舵 | `steer_angle` | ±0.524 rad（±30°）|

> 速度計も GPS も他車レーダーも無い。**「視界」と「残量」だけ**で走る、というのが意図的な制約。

### 1.3 勝利条件と制約

- **目標**: 2 周を最短タイムで完走
- 制約: フライング（GO 前に動く）→ 失格 / コースアウト → 脱落 / SOC 枯渇 → 障害物化
- SOC の減り方: ①走行消費（`|drive_torque|` に比例）+ ②衝突ペナルティ
  → **「速く走る＝電池を使う」というトレードオフ**が常にある

### 1.4 重要な技術的制約（後の議論で効く）

1. **テーブルモードは純粋なオープンループ**
   - CSV を上から 20fps で送るだけ。センサを一切読まない。
   - レッスン 04 の明言: 「同じ CSV でもきれいに再現できる人はほとんどいない。原因は遅延」。
   - 通信ジッタ（localhost で 1〜5ms）＋ 機械的遅延 → **同じ CSV でも毎回ズレる（非決定的）**。

2. **実行中は位置情報が取れない**（`rl_reward.py` の注記より）
   - リアルタイムで得られるのは **SOC とステップ数のみ**。
   - `pos_x, pos_z, yaw, status` は **走行終了後に metadata.csv へ書き出される**だけ。
   - → 「理想のラインを走る」ための自車位置は、走行中には参照できない。

3. **制御ループは 50ms。超過すると前フレームの指令が保持される**（暴走の原因になりうる）。

### 1.5 ログ（metadata.csv）の主要列

`id, session_time_ms, race_time_ms, filename, soc, drive_torque, steer_angle, status, pos_z, pos_x, yaw, pos_y, error_code, collision_type, collision_penalty, collision_target`

- 1 tick = 50ms（1000 行 ≈ 50 秒）
- `steer_angle` は **ラジアン**、`yaw` は **度**（混在に注意）
- `pos_z`（前後）, `pos_x`（左右）の散布図で走行ラインが描ける

### 1.6 自動化の足場（既に存在）

- `scripts/headless_loop.py`: Unity を一度起動し、N エピソードを繰り返す。`--retrain "<cmd>"` でエピソード間に任意スクリプトを実行できる。
- `scripts/optimize_params.py`: 直近 run の metadata.csv からスコアを計算し、ヒルクライムで `rule_based_input.py` のパラメータを 1 つ更新する（既存実装）。
- スコア関数: `score = race_time_s + collision_penalty_total × 10`（低いほど良い、DNF=999999）
- **metadata.csv を得るには `DATA_SAVE=1` が必要**。

---

## 2. テーブルモードの思考実験 — 提案と多角的評価

### 2.1 提案された仮説

> 報酬設計 × 理想のラインを走ることで速くできるのでは。
> ①数周マニュアルで走りデータ取得 → ②そのデータから table_input.csv 作成 → ③走行 →
> ④結果を検証し報酬設計に従い CSV を書き換える → ②へ。強化学習に近い。
> aira は自動実行できるので、レース→解析→CSV変更→レースを最速まで回せるはず。

### 2.2 評価（多角的）

#### ✅ 妥当な点
- 「走る → 計測 → 直す → また走る」という**反復改善ループ**の発想は正しい。
- 自動化の足場（headless_loop + retrain フック）が既にあり、**機械的には実装可能**。
- 「良い走行ラインを基準にする」という着眼点は、制御の本質に近い。

#### ⚠️ 構造的な問題（ここが核心）

1. **オープンループ × 非決定性 → 最適化が「動く的」になる**
   - テーブルは「行番号 N の指令」を送るだけ。N 行目の指令の結果として現れるズレは、
     実際には **0〜N 行の累積ドリフト**が原因。指令と結果の対応（credit assignment）が崩れる。
   - 前回の走行ラインを見て CSV を直しても、次回は別のラインを走る（ジッタで全体がずれる）。
     → **観測できない・再現できない対象を最適化することになり、収束が保証されない**。

2. **「理想ライン追従」に必要な自車位置が走行中に取れない**（1.4-2）
   - テーブルモードで online にラインを追うことは原理的に不可能。
   - できるのは「走り終えた後に metadata を見て、次回用 CSV を作り直す」**オフライン後処理のみ**。

3. **報酬設計の置き場所が違う**
   - 報酬整形（reward shaping）が効くのは、**方策（policy）を学習して汎化する**主体がいる場合
     （= ルールベースのゲイン、または RL/AI モード）。
   - テーブルには方策が無く、固定の「指令テープ」があるだけ。報酬は**走行全体を 1 スカラーで順位付け**
     することしかできず（これは optimize_params.py が既にやっている）、CSV の特定行へ
     報酬を割り当てて直す、という RL 的更新は (1) のため信頼できない。

#### 🎯 テーブルモードが本当に向いていること
- (a) **ベースライン / デモ**: 人間や既存ルールベースの 1 周を「再生」して見せる。
- (b) **オープンループの限界を示す実験**（記事として非常に良い: 「なぜ再生は失敗するか」）。
- (c) **余裕を持たせた手作り最適指令**（レッスンの示唆: "carefully crafted command values with margin"）の上限参照。

### 2.3 結論（ナイーブな RL 的書き換えについて）

> CSV を「前回のラインを見て報酬で直す」式に自動反復させても (1)〜(3) により
> 意味のある収束は望みにくい。報酬を CSV の特定行へ割り当てる credit assignment が
> 非決定性で壊れるため。

### 2.4 数学的アプローチ（モデルベース）— 本トライアルの本命

ナイーブな RL 書き換えの代わりに、**オフラインでモデルを作り、最適な指令列を解析的に求める**。
ユーザのアイデア（XYZ位置から入力の関係を数式化 → 理想ラインに合うステアでCSV生成 →
torque はフル加速、ステアは小さいほど良い、タイム最短、ジッタは繰り返しで吸収）は、
制御工学の 4 つの古典問題に分解できる。

| ユーザのアイデア | 確立された手法 | 内容 |
|------------------|----------------|------|
| ①位置と入力の関係を数式化 | **システム同定（運動学的自転車モデル）** | 実データから L, 速度ダイナミクスを最小二乗で同定 |
| ②理想ライン→ステア | **微分平坦性 / 逆モデル** | `δ = arctan(L·κ)` で操舵を**解析的に逆算（反復不要）** |
| ③torque は常に 1 | **最小時間制御は bang-bang** | 全開は半分正しい。**カーブ手前の減速が必須**（でないと脱落） |
| ④繰り返しでジッタ吸収 | **反復学習制御 (ILC)** | 試行間誤差で FF 指令を更新。非決定性に理論的に対処 |

#### 各手法の要点

- **① システム同定**: `yaw_rate = (v/L)·tan(δ)`（操舵→旋回）、`v̇ = a·torque − b·v − c`（速度）。
  操舵指令と yaw 応答の相互相関でレイテンシ（遅延フレーム）も推定。
- **② 微分平坦性**: 自転車モデルは differentially flat。目標ラインの曲率 κ(s) から
  操舵 δ を一発で逆算できる = 「ステアとポジションの関係で CSV を作る」の数学的正体。
- **③ 最小時間最適制御**: 解は bang-bang（ポントリャーギン）。直線全開＋カーブ手前で全制動。
  速度プロファイル（どこで緩めるか）が時間短縮の本体。
- **④ ILC**: 反復タスク × オープンループ FF 指令 × 試行間誤差更新 × モデル誤差耐性。
  「繰り返しシミュレーションで幅を持たせる」の理論的な正当化。

#### その他の手法（比較対象）
- **軌道最適化（直接コロケーション, CasADi 等）**: 「境界内で最小時間」を非線形最適化で直接求解。
  レースの最適ライン問題そのもの（TUM global_racetrajectory_optimization）。
- **CMA-ES / ベイズ最適化**: ラインを少数のスプライン制御点で表現し最適化。
  ノイズのある目的関数に強く、行単位ヒルクライムより非決定性に頑健。

#### 正直な限界（記事の核）
- ②の非決定性により、完璧な FF でも**再現性に上限**。ILC / マージンで緩和、消去はできない。
- **コース境界の明示データが無い**。走行可能領域は過去 run から推定するしかない。
- 「torque 常に 1」は実データ（最速 run でも 0.33〜0.56）と矛盾 → 速度計画が必要。

---

## 3. ルールベースモードの現状と改善余地

### 3.1 現行アルゴリズム（P 制御）

```
カメラ画像 → sliding_windows.py（白線検出）→ lateral_px, theta_deg
          → driver_model.py（P制御）
              steer = k_theta × theta + k_lateral × lateral
              drive = v_max × (1 - steer_cost × 0.7)
          → Unity
```

### 3.2 既存トライアル結果（report_rule_based_trial_01.md）

- ヒルクライムで 4 パラメータ（v_max, k_theta, k_lateral, alpha_smooth）を自動調整
- 結果レンジ: **46.1s（最速）〜 54.8s**、平均 ~50s、DNF 2 回
- 採用: k_lateral 0.60→0.50、k_theta 0.90→0.80（最速ランは未評価で best に未反映）
- **報告書の結論**: P 制御のパラメータ調整だけでは伸びが小さい。
  制御アルゴリズム自体の見直し（PD/PID、前方注視、速度プロファイル分離）が鍵。

### 3.3 改善の方向性

- **A. 制御理論**: P → PD/PID（D 項でカーブ応答を先読み）、直線全開＋カーブ手前で早期減速
- **B. 知覚**: 前方注視点の導入、曲率の直接推定（多項式 2 次項）→ 速度計画
- **C. 探索戦略**: グリッドサーチ、探索範囲拡大

---

## 4. 今日の実験計画（テーブルモード集中・モデルベース）

> 方針決定（2026-06-06）: **今日はテーブルモードの検証に集中**。ルールベース改善は次回。
> ナイーブな RL ではなく、**モデルベース（システム同定 → 微分平坦逆算 → 再現性計測 → ILC）**で進める。

### Phase 1: オフライン解析（Unity 不要・即着手可）
1. **評価/軌跡ツールキット**: metadata.csv を読み、軌跡（pos_z–pos_x）、タイム、衝突、ジッタ、SOC を算出。
2. **システム同定**: 16 run から自転車モデル（L, 速度ダイナミクス, レイテンシ）を最小二乗で同定。
   - 成果物: 同定パラメータ + 当てはまりの良さ（R²）+ 軌跡プロット。

### Phase 2: フィードフォワード CSV 生成と実走（要 Unity・要 go ahead）
3. **理想ライン仮決定**: 既存最速 run のラインを基準に、平滑化/最小曲率で参照ラインを作る。
4. **微分平坦で逆算**: `δ = arctan(L·κ)` で操舵列、bang-bang + 速度プロファイルで torque 列 → `table_input.csv`。
5. **設定**: `config.txt` を `ACTIVE_ROBOTS=1`, `R1_MODE_NUM=2`(table), `DATA_SAVE=1` に。
6. **実走 & 再現性計測**: 同一 CSV を複数回再生し、ラインのばらつき（再現性ギャップ [m]）を定量化。

### Phase 3: ILC（任意・時間が許せば）
7. 試行間の軌跡誤差で操舵 FF を 1〜2 回更新し、収束 or 限界を観測。

### 4.4 記事化
- 「ナイーブ RL ではなくモデルベースで解く」「微分平坦で反復なしに操舵が出る」
  「だが非決定性で再現性に上限」という流れをデータで語る。

---

### 4.5 Phase 1 結果（2026-06-06, 実データ16run）

`scripts/table_model.py` によるシステム同定。

| 項目 | 結果 | 解釈 |
|------|------|------|
| 軸距 L | **0.274 m** | — |
| 操舵→旋回 R² | **0.807** | `yaw_rate=(v/L)·tan(δ)` 成立 → 微分平坦逆算が妥当 |
| 速度モデル R² | 0.303 | 弱い。位置差分速度はノイズ大、単純1次では不足 |
| 全開終端速度 | ~1.26 m/s | 観測最大(~1.1)と整合 |
| レイテンシ | 0 frame (<50ms) | ジッタはサブフレーム（localhost 1〜5ms と一致）|

**発見**:
1. 操舵モデルが良く当たる → 「位置とステアの関係の数式化」は数学的に妥当。
2. 速度モデルは弱い → torque 計画は bang-bang + 保守マージンで補う必要。
3. 最速 run でも平均 torque 0.29 / 平均|steer| 0.35 → **フルスロットルでは走っていない**。
4. DNF は v_mean 0.09〜0.21 のスタック → 完走可否がスコアを支配。

成果物: `scripts/table_model.py`, `scripts/table_model_params.json`

### 4.6 Phase 2（オフライン）結果 — 逆モデル自己検証

`scripts/make_table_csv.py` で、参照run(165600)の**記録ラインの曲率から操舵を逆算**し、
記録操舵と突き合わせた（Unity実走前のモデル検証）。

| 平滑化窓 | RMSE | corr |
|---------|------|------|
| 5 | 15.3° | 0.665 |
| 11 | 13.4° | 0.684 |
| 21 | 15.5° | 0.543 |
| 31 | 14.0° | 0.630 |

**発見（記事の核 2）**:
- 集計回帰では操舵→旋回が R²=0.81 で合うのに、**記録ライン曲率からの逆算は corr~0.68 が限界**。
- 原因: 記録ラインは **P制御で揺れた軌跡**。2階微分すると揺れが偽の曲率になる。
- → 正しいのは「記録ライン曲率」ではなく **「滑らかな理想ラインを設計 → その κ で δ=atan(Lκ)」**。
  = ユーザの「理想レースレコードラインを仮決定」が必須ステップである、と実証された。

**Unity実走の順序**: モデル化リスクの無い **replay（純再生）で再現性ギャップを先に測る** →
その後、滑らかな理想ラインで flatsteer/model を試す。

生成器の3モード: `replay` / `flatsteer` / `model`（→ `scripts/generated_tables/`）。
成果物: `scripts/make_table_csv.py`

---

## 4.7 Phase 2（実走）結果 — replay 5回の再現性（★本トライアルの主結果）

最速 run(165600, 46.1s) の記録指令をそのまま `table_input.csv` にして、
**同一CSVを5回 table モードで再生**（`headless_loop.py 5`、Robot1単独、DATA_SAVE=1）。

### ラップ構造（決定的な発見）

| run | Lap0→Lap1 | Lap1→Finish | 合計 | 備考 |
|-----|-----------|-------------|------|------|
| 091101 | 21.5s | **1.3s** | 22.9s | |
| 091140 | 19.6s | **4.0s** | 23.6s | |
| 091221 | 19.6s | **4.5s** | 24.1s | |
| 091302 | 19.8s | **5.3s** | 25.1s | |
| 091344 | -- | -- | 12.7s | **Lap1 を記録せず** |
| **参照 165600** | 19.3s | **26.8s** | 46.1s | 元のルールベース走行 |

> **【重要・解釈の訂正】** 当初「2周目に判定線を早期横断して偽完走した」と解釈したが、
> これは誤り。**実際は5本とも車がコースアウトして落下（Fallen）していた**（目視＋データで確認）。
> 全終端行が `pos_y ≈ -0.10〜-0.106`（仕様上 `pos_y<-0.1`＝コースアウト）。
> ただし **CSVの `status` は誤って `Finish` と記録されるシミュレータ側バグ**があり（→ `csv-fallen-comment.md` で本家へ報告）、
> ラップ表の「Lap1→Finish 1.3〜5.3s」は実際には「Lap1 通過後まもなく落下した時刻」を指す。

### 解釈：オープンループは「1周は持つが、その後ドリフトでコースアウトする」

1. **1周目（Lap0→Lap1）は忠実に再現**: replay 4本が 19.6〜21.5s で、参照 19.3s とほぼ一致。
   この区間の**5本の軌跡ばらつきは平均 8.7cm / 最大 29cm**（共通窓 0〜12.7s）。
   → 短時間（〜20s, 1周）ならオープンループ再生は概ね機能する。
2. **1周目以降に崩壊＝コースアウト**: 参照は2周目に 26.8s かけて完走するが、replay は
   Lap1 通過後 1.3〜5.3s で `pos_y<-0.1` に達して**落下**（091344 は Lap1 前に 12.7s で落下）。
   蓄積したドリフトがコース外への逸脱を生んだ。
3. **タイムばらつきは巨大**: 落下までの時間が 12.7〜25.1s（std 4.55s）。同一入力なのにこの分散。
4. 参照ライン（165600）からの逸脱: 平均 13.9cm / 最大 65.8cm。

### 結論（記事の主張）

> **オープンループ再生の誤差は「一様なブレ」ではなく「ホライズン依存の崩壊」**である。
> 短期（1周）は cm オーダーで追従するが、ドリフトが蓄積するとやがて**コースアウトして落下**し、
> 結果が無意味になる。これはレッスン04の警告「初期誤差とジッタは無補正で蓄積する」をデータで実証したもの。
> → **テーブルモードの限界は明確。速く・安定して走るにはクローズドループ（フィードバック）が必須**。
>
> 副産物として、**status を常に `Finish` と書く重大なログバグ**を発見・報告した（`csv-fallen-comment.md`）。
> このバグは自動最適化を直接破壊する（落下＝短時間＝高スコアとなり、optimizer がクラッシュを「最良」と誤学習する）。

成果物: `scripts/analyze_repro.py`、`csv-fallen-comment.md`、runs: `run_20260606_091101`〜`091344`

---

## 5. オープン課題 / 検証メモ

- [x] テーブル再生の再現性ギャップは実測どの程度か → **1周目 平均8.7cm/最大29cm、2周目で崩壊**（4.7）
- [x] DATA_SAVE=1 時、テーブルモードでも metadata.csv は出るか → **出る**（位置・status含む）
- [ ] 「滑らかな理想ライン」設計 → flatsteer/model で replay を超えられるか（次の一手）
- [ ] ルールベース PD 化で 46s をどこまで縮められるか（次回）
- [ ] スコア関数の重み（×10）の妥当性
- [ ] replay の2周目崩壊が「ドリフト→判定線早期横断」である軌跡的裏付け（matplotlib導入で可視化）
